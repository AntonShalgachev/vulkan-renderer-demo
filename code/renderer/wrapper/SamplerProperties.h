#pragma once

namespace vko
{
    enum class SamplerFilterMode
    {
        Nearest,
        Linear,
    };

    enum class SamplerWrapMode
    {
        Repeat,
        Mirror,
        ClampToEdge,
    };
}
